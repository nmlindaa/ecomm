import { css } from '@emotion/react'

export const orderReviewModalStyle = {
  wrapper: css({
    padding: '16px',
  }),
  listItem: css({
    display: 'flex',
    justifyContent: 'space-between',
  }),
}
