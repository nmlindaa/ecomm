import { css } from '@emotion/react'

export const homeStyle = {
  wrapper: css({
    padding: '24px',
    fontSize: '16px',
    lineHeight: 1.5,
  }),

  item: css({
    marginBottom: '12px',
  }),

  button: css({
    paddingTop: '24px',
    display: 'flex',
    justifyContent: 'center',
  }),
}
