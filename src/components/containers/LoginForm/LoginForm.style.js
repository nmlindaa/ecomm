import { css } from '@emotion/react'

export const loginStyle = {
  wrapper: css({
    margin: '200px 48px',
  }),

  button: css({
    paddingTop: '8px',
    display: 'flex',
    justifyContent: 'center',
  }),
}
